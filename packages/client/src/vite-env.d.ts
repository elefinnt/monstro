/// <reference types="vite/client" />

// @fontsource packages ship CSS only (no type declarations); allow the
// side-effect import that registers the @font-face.
declare module "@fontsource/press-start-2p";
